# Changelog

[English](CHANGELOG.md) · [中文](CHANGELOG.zh-CN.md)

本文件记录 AutoToggle 每个版本的重要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [2.0.0] - 2026-08-16

### 新增

- 内置 Sparkle 2 自动更新：启动时自动检查，也可在「设置 → 关于 → 检查更新…」手动触发；新版自动下载安装

## [1.2.0] - 2026-08-15

### 新增

- 完整英文界面：可在「设置 → 语言」切换中文 / English / 跟随系统（重启应用后生效）

### 修复

- 修复切换为英文后界面仍全中文的问题：根因是语言设置未同步到 `AppleLanguages`、且大量界面文案缺少英文翻译；现已接入语言覆盖并补齐全部英文翻译
- 修复菜单栏下拉面板顶部箭头颜色与主体不一致：移除叠加在面板上的不透明 `windowBackground`，让 `NSPopover` 自身材质（含箭头）透出；同时把浅/深色外观设置接入 `NSApp.appearance`
- 修复活动日志重复记录：同一动作不再产生「退出应用/启动应用 + 闲置退出/定时启动」两条记录；触发型动作只记语义化条目（闲置退出/定时启动/定时退出/闲置隐藏），手动退出由菜单栏单独记录
- 菜单栏退出按钮颜色加深（改用主色，与灰色功能按钮区分）；日志分类选择器移除固定宽度，避免英文标签被截断

## [1.1.0] - 2026-08-14

### 新增

- 应用选择器完整扫描 `/Applications/Utilities`（终端、活动监视器等），并按 Bundle ID 去重
- 应用列表按界面语言排序（升序）：中文界面按拼音、英文界面按字母（总览被管理应用 + 应用选择器）
- 应用选择器右侧字母表支持点击跳转，搜索框打开时自动聚焦
- 闲置时长的「自定义」改为手动输入分钟数（支持键入与步进器）
- 设置界面新增「辅助功能权限」状态展示与一键请求
- 菜单栏与被管理应用并列展示「定时任务」（定时启动/退出 + 相对时间），禁用（关闭）的定时任务同样显示，可通过右侧开关切换启用状态

### 变更

- 菜单栏图标改用 `NSStatusItem` + `NSPopover(.transient)`，修复打开菜单栏时激活 app、抢占其它应用键盘焦点的问题（面板按钮可正常点击）
- 从菜单栏打开主界面时先收起面板，并把主窗口置前（`.activateIgnoringOtherApps` + `orderFrontRegardless`，关闭后也能重开）
- 辅助功能权限状态改为周期性重查（3 秒），授权后能及时同步
- 点击主窗口红色关闭键后，应用从 Dock 消失（转为纯菜单栏模式）；点击菜单栏「主界面」重新显示并恢复 Dock 图标
- Release 构建改用稳定的自签名证书签名（`scripts/bootstrap-signing.sh` 生成，非 ad-hoc），使辅助功能授权在每次重建后不再失效；Debug 保持 ad-hoc 以便调试器附加

### 修复

- 修复定时启动的应用在几十秒内被误判为「闲置」而退出的问题：应用启动时重置其闲置基线，避免陈旧的活跃时间让闲置规则立即触发（如百度网盘定时打开后几十秒即被闲置退出）
- 修复「系统设置中辅助功能已授权、但应用内始终显示未授权」：根因是 ad-hoc 签名的 designated requirement 为 cdhash，每次重建都变导致 TCC 授权失效；改用稳定自签名证书后身份只依赖 bundle id + 证书，重建不再变化
- 授权模块重构：抽出代码签名诊断（判断是否 ad-hoc 签名），未授权时输出可操作的日志，便于定位授权失效根因
- 修复「运行时授予辅助功能后应用仍显示未授权」：macOS 对运行中进程缓存授权状态，授权后需重启才能生效。现在从系统设置返回后若仍显示未授权，会提示「重启应用以生效」并提供一键重启

## [1.0.0] - 2026-08-13

首个公开发布版本。

### 新增

- 定时规则：按具体时间和星期自动启动或退出应用
- 闲置检测：应用闲置指定时长后自动退出或隐藏
- 假闲置检测：智能识别音频播放、会议等场景，避免误关
- 菜单栏驻留：安静运行在菜单栏，不占用 Dock 空间
- 开机自启：登录时自动启动，无需手动操作
- 隐私优先：所有数据存储在本地，无网络请求、无数据上传
- 中文原生界面，对微信 / 钉钉 / 飞书等国内应用优化
- 应用退出三级优雅降级策略（AppleScript quit → terminate → SIGTERM）

### 安全

- 启用 Hardened Runtime 与最小化授权
- 导入配置文件增加大小与字段范围校验

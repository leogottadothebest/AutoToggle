# AutoToggle 隐私政策

> AutoToggle 是一款「隐私优先」的 macOS 菜单栏工具。本文件说明它收集什么、不收集什么、以及数据如何存储。

## 我们不收集或上传任何数据

- **无分析、无埋点、无崩溃上报**：AutoToggle 不集成任何第三方分析 SDK，也不上传任何使用数据或崩溃日志。
- **数据不离开设备**：所有规则、日志和设置都只存储在你这台 Mac 本地。

## 本地存储

- 你的规则（定时启动/退出、闲置退出/隐藏）、日志与配置方案存储在本地 SwiftData 数据库中，位置为 `~/Library/Application Support/default.store`。
- 这些数据不会同步、不会上传，也不会被我们访问。

## 唯一的网络行为

- 仅当启用自动更新时，AutoToggle 会通过 HTTPS 向 Sparkle 更新源（`appcast.xml`）发起一次**更新检查**请求，用于判断是否有新版本。
- 你可以在「设置 → 关于」关闭「自动检查更新」，此后应用完全离线。
- 更新检查不携带任何规则、日志或个人数据。

## 权限用途

- **辅助功能（可选）**：用于实时跟踪当前前台应用，提升闲置判定精度。未授权不影响核心功能。
- **Apple Events 自动化**：仅在规则触发时向目标应用发送「退出/隐藏」命令，实现定时或闲置自动退出。

## 诊断导出（可选）

- 应用内「导出诊断…」仅在你主动点击时，把版本、权限状态、本地日志打包成一个 zip 供你自行决定是否附加到 Issue；**不会自动上传**。

## 联系我们

如有隐私相关问题，请通过 GitHub Issue 联系我们。
